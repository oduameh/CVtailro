# ============================================================================
# CVtailro Environment Variables
# ============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# See SETUP.md for detailed instructions on obtaining each credential.
# ============================================================================

# --- Flask -------------------------------------------------------------------

# Secret key for signing session cookies. MUST be a strong random string in
# production. Generate one with:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=change-me-to-a-random-string

# Flask application entry point (set on Railway, optional locally)
FLASK_APP=app.py

# Port the server listens on (default: 5050, Railway sets this automatically)
# PORT=5050

# --- Database ----------------------------------------------------------------

# PostgreSQL connection string. Auto-injected by Railway when you add the
# PostgreSQL addon. For docker-compose, this is set in docker-compose.yml.
# If not set, the app falls back to SQLite (cvtailro_dev.db) for local dev.
DATABASE_URL=postgresql://cvtailro:cvtailro@localhost:5432/cvtailro

# --- Google OAuth 2.0 -------------------------------------------------------
# Required for user authentication. Create credentials at:
#   https://console.cloud.google.com/apis/credentials

GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# --- Admin -------------------------------------------------------------------

# Comma-separated email addresses that receive admin privileges on Google login.
# Example: alice@gmail.com,bob@gmail.com
ADMIN_EMAILS=admin@example.com

# Password for the /admin configuration panel. If not set, the first visit
# to /admin will prompt you to create one interactively.
ADMIN_PASSWORD=

# --- OpenRouter API ----------------------------------------------------------
# API key for the LLM backend (supports Claude, GPT-4o, Gemini, Llama, etc.).
# Get your key at: https://openrouter.ai/keys
# Can also be configured via the admin panel at /admin after deployment.

OPENROUTER_API_KEY=sk-or-v1-your-key-here

# --- Cloudflare R2 Storage (Optional) ---------------------------------------
# Stores generated PDFs and artifacts in Cloudflare R2 (S3-compatible).
# If not configured, the app falls back to local file storage.
# See SETUP.md Section 2 for setup instructions.

R2_ACCOUNT_ID=your-cloudflare-account-id
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
R2_BUCKET_NAME=cvtailro
